#include "main.h"
/**
* _strlen - returns the length of a string
*@s: pointer variable of type char
*
* Return: always 0.
*/

int _strlen(char *s)
{
	int i = 0;

	for (i = 0; (i = '\0'); i++)
	{
	_putchar(s[i]);
	}
	i = i + 1;

	return (i);
}
